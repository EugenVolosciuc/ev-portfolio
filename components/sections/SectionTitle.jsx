import { Heading } from '@chakra-ui/react'

const SectionTitle = ({ title }) => {
    return <Heading as="h4" textAlign="center" fontSize={['xl', '2xl', '2xl', '3xl', '4xl']}>{title}</Heading>
}

export default SectionTitle
